const config = {
    // 启动端口
    port: 5000,

    // 数据库配置
    database: {
        DATABASE: 'aperture',
        USERNAME: 'root',
        PASSWORD: '19951017',
        PORT: '3306',
        HOST: '127.0.0.1'
    }
};

module.exports = config